export default () => ({
    graphql: {
        config: {
          defaultLimit: 50, // Set default limit (adjust as needed)
          maxLimit: 1000, // Set a maximum limit (optional)
        },
      },    
});
